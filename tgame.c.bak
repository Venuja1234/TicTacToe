#include "tictactoe.h"

int main() {
    int n = 3;
    char** board = initializingBoard(n);
    
    printf("=== Tic-Tac-Toe Game ===\n");
    displayingBoard(board, n);
    
    freeBoard(board, n);
    return 0;
}
